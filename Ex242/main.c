#include <stdio.h>
#include <stdlib.h>

/*
Exercício 242: Lista simplesmente encadeada, lista duplamente encadeada e lista circular

->Uma lista, também chamada de lista encadeada ou ainda, lista simplesmente encadeada
é uma sequência de elementos encadeados, um após o outro.

->A diferença de uma lista pra pilhas e filas se baseia nas restrições,
onde a restrição de uma pilha é a inserção/remoção somente pelo topo,
e de uma fila inserção no fim, remoção no início.

->Em uma lista podemos:

Inserir: início/meio/fim
Remover: início/meio/fim
Fazer buscas
Fazer inserções ordenadas

Lista Simpletesmente Encadeada

-> Elementos sequênciais como uma pilha/fila
->Nó - Próximo Nó

Lista Duplamente Encadeada

->Cada elemento é ligado ao seu elemento anterior
->Nó - Nó Anterior - Próximo Nó

Lista Circular

->O último elemento aponta para o primeiro elemento da lista
->Quando o ultimo nó apontar para o início da lista, será o final da lista circular

ADENDOS:

->Ordem de execução dos nós de uma lista: aloca->preenche->insere
->Não pode perder a referência para o início da lista
->Criar um ponteiro auxiliar para o primeiro nó
->Percursão da lista com um ponteiro auxiliar
->Inserção de nós em qualquer posição, sem restrição
->Remoção em lista é mais complicada por não possuir uma restrição
sendo necessário realizar busca
*/

int main()
{
    return 0;
}
